name: gptom_aidl_plugin
description: "A new Kreiseck plugin project."
version: 0.0.2
homepage:

environment:
  sdk: ^3.6.0
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  app_links: ^6.4.1
  plugin_platform_interface: ^2.1.8
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.kreiseck.gptom_aidl_plugin
        pluginClass: GptomAidlPlugin